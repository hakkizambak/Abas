public class third_question {
    int productId;
    double total_price;
    int total_amount;

    public third_question(int productId, double total_price, int total_amount) {
        this.productId = productId;
        this.total_price = total_price;
        this.total_amount = total_amount;
    }
}